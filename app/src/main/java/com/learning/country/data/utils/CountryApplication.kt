package com.learning.country.data.utils

import android.app.Application
import com.learning.country.room.CountryDataBase

class CountryApplication : Application() {
    //val database by lazy { CountryDataBase.getDatabase(this) }
    //val repository by lazy { CountryRepository(database.wordDao()) }
    /*
    * now go to manifest file and update application name to uor class
    *
    * */
}